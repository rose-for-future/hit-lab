{
  "_comment": "hit-lab SessionStart hook. Auto-renders 4-6 line status report at every new conversation. Synchronous so the report appears in Claude's context BEFORE first reply. Silently exits if not in a hit-lab project (no .hit-state.json found).",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PROJECT_DIR}/.hit-hooks/session-start.sh",
            "timeout": 5,
            "async": false
          }
        ]
      }
    ]
  }
}
