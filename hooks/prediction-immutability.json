{
  "_comment": "hit-lab prediction immutability hook. Blocks edits to '## 预测' / '## Prediction' sections in predictions/*.md files. Merge this into your project's .claude/settings.json on /hit-init.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PROJECT_DIR}/.hit-hooks/prediction-immutability.sh",
            "timeout": 3,
            "async": false
          }
        ]
      }
    ]
  }
}
